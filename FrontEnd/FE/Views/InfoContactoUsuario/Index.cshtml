@model IEnumerable<FE.Models.InfoContactoUsuario>

@{
    ViewData["Title"] = "Index";
}
<p>
    <a asp-action="Create">Crear nuevo</a>
</p>

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">Informacion de contacto</h3>

            </div>
            <!-- /.card-header -->
            <div class="card-body table-responsive p-0">
                <table class="table table-hover text-nowrap">
                    <thead>
                        <tr>
                            <th>Nombre</th>
                            <th>Telefono Contacto</th>
                            <th>parentesco</th>
                            <th>Usuario</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.NombreCompletoContacto)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.TelefonoContacto)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.IdRolCont)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.IdUsuario)
                                </td>
                                <td>
                                    <a asp-action="Edit" asp-route-id="@item.IdInfo">Editar</a> |
                                    <a asp-action="Details" asp-route-id="@item.IdInfo">Details</a> |
                                    <a asp-action="Delete" asp-route-id="@item.IdInfo">Eliminar</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
            <!-- /.card-body -->
        </div>
        <!-- /.card -->
    </div>
</div>
